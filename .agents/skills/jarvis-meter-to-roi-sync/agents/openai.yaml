interface:
  display_name: "Jarvis Meter To ROI Sync"
  short_description: "Ingest meter photos and sync ROI labels in one run."
  default_prompt: "Process new meter photos end-to-end: remove sidecars, rename from EXIF, read register values, upsert meter_readings.csv, then sync and label new meter_* files into backend/data/roi_dataset with QA review."
