interface:
  display_name: "Jarvis Meter To ROI Sync"
  short_description: "Ingest meter photos and sync ROI labels in one run."
  default_prompt: "Use $jarvis-meter-to-roi-sync to ingest new meter photos, collect and validate manual YOLO ROI labels, rebuild the ROI dataset, stop for overlay approval, and publish only the approved DVC artifacts."
